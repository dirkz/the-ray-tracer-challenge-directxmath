#pragma once

#include "stdafx.h"

namespace zrt
{

constexpr float PI = std::numbers::pi_v<float>;
constexpr float PIFifth = std::numbers::pi_v<float> / 5.f;
const float HalfSqrt = sqrt(2.f) / 2;

} // namespace zrt
