#pragma once

#include "stdafx.h"

namespace zrt
{

constexpr float PI = std::numbers::pi_v<float>;
constexpr float FifthPI = std::numbers::pi_v<float> / 5.f;
constexpr float HalfPI = std::numbers::pi_v<float> / 2.f;
constexpr float QuarterPI = std::numbers::pi_v<float> / 4.f;
const float HalfSqrt = sqrt(2.f) / 2;

} // namespace zrt
